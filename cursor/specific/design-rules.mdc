---
alwaysApply: true
---

# Direction Artistique - Scrountch App

## 🎨 Philosophie Design

L'application Scrountch suit une direction artistique **"Jaune Bold"** caractérisée par :

- Des nuances de jaune comme couleur dominante
- Une typographie ultra-grasse et impactante
- Des interfaces minimalistes sans fioritures
- Une cohérence visuelle forte entre tous les écrans
- Des contrastes forts entre éléments noirs et fonds jaunes

## 🌈 Palette de Couleurs

### Couleur Principale Unifiée (Fonds d'écrans)

```css
Jaune Uniforme:     #FFE333  /* TOUTES les pages utilisent cette couleur */
```

**⚠️ IMPORTANT** : Toutes les pages utilisent maintenant la même couleur de fond `#FFE333` pour une cohérence parfaite.

### Couleurs Secondaires

```css
Noir:        #000000  /* Texte, bordures, icônes */
Transparent: transparent /* Fonds de boutons et inputs */
Noir 54%:    Colors.black54 /* Placeholders */
Blanc:       #FFFFFF  /* Texte sur cartes colorées */
Rouge:       #FF0000  /* Cartes d'items (couleur actuelle) */
```

## 🔤 RÈGLES DES POLICES

### ⚠️ SEULES 2 POLICES AUTORISÉES

#### 1. **Dela Gothic One**

- **Usage** : TITRES DE PAGES + TOUS LES BOUTONS (en CAPITALES)
- **Fichier** : `assets/fonts/DelaGothicOne-Regular.ttf`
- **Style** : Bold naturellement, moderne et distinctif
- **Exemples** : "TROUVER", "RANGER", "RECHERCHER", "NOUVEL OBJET", "ANNULER", "ENREGISTRER"

#### 2. **Chivo Regular**

- **Usage** : TOUT LE RESTE (texte courant, champs, labels, hints)
- **Fichier** : `assets/fonts/Chivo-Regular.ttf`
- **Style** : Regular uniquement, lisible et élégant
- **Exemples** : Champs de texte, descriptions, labels de formulaires, texte courant

### 🚫 INTERDICTIONS ABSOLUES

- **AUCUNE AUTRE POLICE** n'est acceptée
- Pas de polices système (System, San Francisco, Roboto, etc.)
- Pas d'autres Google Fonts
- Pas de mélange avec d'autres familles

## 🎯 SYSTÈME DE BOUTONS GLOBALISÉ

### 📋 3 Variants de Boutons Standardisés

#### 1. **Primary Button** - Actions Principales

```dart
// Usage: RECHERCHER, ENREGISTRER, MODIFIER, CHANGER LA RECHERCHE
PrimaryButton(
  onPressed: () => action(),
  text: 'TEXTE EN MAJUSCULES',
  iconPath: 'assets/images/icon.png', // Optionnel
)

// Style automatique:
// - Fond: Colors.black
// - Texte/Icône: Color(0xFFFFE333) (jaune)
// - Police: DelaGothicOne, 24px
// - Bordure: Aucune
// - Hauteur: 75px
```

#### 2. **Secondary Button** - Actions Secondaires

```dart
// Usage: RANGER, SUPPRIMER, ANNULER, RANGER CET OBJET
SecondaryButton(
  onPressed: () => action(),
  text: 'TEXTE EN MAJUSCULES',
  iconPath: 'assets/images/icon.png', // Optionnel
)

// Style automatique:
// - Fond: Color(0xFFFFE333) (jaune)
// - Texte/Icône: Colors.black
// - Police: DelaGothicOne, 24px
// - Bordure: Noire 1px
// - Hauteur: 75px
```

#### 3. **Tertiary Button** - Dialogues de Confirmation

```dart
// Usage: Boutons dans AlertDialog uniquement
TertiaryButton(
  onPressed: () => action(),
  text: 'TEXTE EN MAJUSCULES',
  iconPath: 'assets/images/icon.png', // Optionnel
)

// Style automatique:
// - Fond: Color(0xFFFFE333) (jaune)
// - Texte/Icône: Colors.black
// - Police: DelaGothicOne, 19.2px (24px × 0.8)
// - Bordure: Aucune
// - Hauteur: 50px
```

### 📁 Fichiers de Styles Centralisés

#### `/lib/theme/button_styles.dart`

```dart
class AppButtonStyles {
  static ButtonStyle get primary    // Fond noir, texte jaune
  static ButtonStyle get secondary  // Fond jaune, texte noir, bordure
  static ButtonStyle get tertiary   // Fond jaune, texte noir, sans bordure
  static const TextStyle buttonTextStyle // DelaGothicOne, 24px
  static Widget buildButtonContent(...) // Helper pour icône + texte
}
```

#### `/lib/widgets/custom_buttons.dart`

```dart
class PrimaryButton extends StatelessWidget   // Bouton primary
class SecondaryButton extends StatelessWidget // Bouton secondary
class TertiaryButton extends StatelessWidget  // Bouton tertiary (taille réduite)
```

### 🎨 Mapping des Boutons par Écran

| Écran             | Bouton               | Type      | Icône           |
| ----------------- | -------------------- | --------- | --------------- |
| **Home**          | TROUVER              | Primary   | search_icon.png |
| **Home**          | RANGER               | Secondary | store_icon.png  |
| **Search**        | RECHERCHER           | Primary   | search_icon.png |
| **Form**          | ENREGISTRER          | Primary   | check_icon.png  |
| **Form**          | ANNULER              | Secondary | cross_icon.png  |
| **Detail**        | MODIFIER             | Primary   | pen_icon.png    |
| **Detail**        | SUPPRIMER            | Secondary | trash_icon.png  |
| **Detail Dialog** | SUPPRIMER            | Tertiary  | trash_icon.png  |
| **Detail Dialog** | ANNULER              | Tertiary  | cross_icon.png  |
| **No Results**    | CHANGER LA RECHERCHE | Primary   | search_icon.png |
| **No Results**    | RANGER CET OBJET     | Secondary | plus_icon.png   |

## 📱 STRUCTURE D'ÉCRAN STANDARDISÉE

### 🏗️ Layout Uniforme

Tous les écrans suivent cette structure :

```dart
Scaffold(
  backgroundColor: const Color(0xFFFFE333), // Jaune uniforme
  body: Stack(
    children: [
      // Image de fond avec opacité
      Positioned.fill(
        child: Opacity(
          opacity: 0.2, // 20% d'opacité
          child: Image.asset('assets/images/search_bg.png'),
        ),
      ),

      // Contenu principal
      SafeArea(
        child: Padding(
          padding: const EdgeInsets.symmetric(vertical: 24.0), // 24px vertical
          child: Column(
            children: [
              // Header standardisé
              Padding(
                padding: const EdgeInsets.symmetric(horizontal: 24.0),
                child: Row(
                  children: [
                    // Bouton retour (50x50px)
                    GestureDetector(
                      onTap: () => Navigator.pop(context),
                      child: Image.asset(
                        'assets/images/back_icon.png',
                        width: 50,
                        height: 50,
                      ),
                    ),
                    const Spacer(),
                    // Bouton home (50x50px)
                    GestureDetector(
                      onTap: () => Navigator.pushAndRemoveUntil(...),
                      child: Image.asset(
                        'assets/images/home_icon.png',
                        width: 50,
                        height: 50,
                      ),
                    ),
                  ],
                ),
              ),

              // Titre de la page
              Padding(
                padding: const EdgeInsets.symmetric(horizontal: 24.0),
                child: Text(
                  'TITRE EN MAJUSCULES',
                  textAlign: TextAlign.center,
                  style: const TextStyle(
                    fontFamily: 'DelaGothicOne',
                    fontSize: 38,
                    color: Colors.black,
                    height: 1.1,
                  ),
                ),
              ),

              // Contenu spécifique à l'écran...
            ],
          ),
        ),
      ),
    ],
  ),
)
```

### 📏 Espacements Standardisés

```dart
// Padding global des écrans
vertical: 24px    // Haut et bas de chaque écran
horizontal: 24px  // Gauche et droite pour header et titres

// Tailles des icônes header
back_icon.png: 50x50px
home_icon.png: 50x50px

// Tailles des icônes boutons
Boutons principaux: 45x45px
Boutons dialogues: 40x40px

// Hauteurs des boutons
Primary/Secondary: 75px
Tertiary: 50px
```

## 🔧 INPUTS ET FORMULAIRES GLOBALISÉS

### 📝 CustomTextField Widget

```dart
// Usage standardisé
CustomTextField(
  controller: controller,
  labelText: 'Label',
  hintText: 'Placeholder',
  fillColor: const Color(0xFFFFE333), // Fond jaune si nécessaire
  filled: true,
)

// Style automatique depuis main.dart:
// - Police: Chivo, 18px
// - Bordure: Noire, arrondie 8px
// - Padding: 12px vertical, 16px horizontal
```

### 🎛️ Configuration Globale (main.dart)

```dart
inputDecorationTheme: InputDecorationTheme(
  contentPadding: EdgeInsets.symmetric(vertical: 12, horizontal: 16),
  labelStyle: TextStyle(fontFamily: 'Chivo', fontSize: 18, color: Colors.black),
  hintStyle: TextStyle(fontFamily: 'Chivo', fontSize: 18, color: Colors.grey),
  border: OutlineInputBorder(
    borderRadius: BorderRadius.circular(8),
    borderSide: BorderSide(color: Colors.black, width: 1),
  ),
),

textTheme: TextTheme(
  bodySmall: TextStyle( // Style pour le texte des inputs
    fontFamily: 'Chivo',
    fontSize: 18,
    fontWeight: FontWeight.w500,
    color: Colors.black,
  ),
),
```

## 📋 CHECKLIST DE CONFORMITÉ

Avant de valider un écran, vérifier :

### 🎨 Design

- [ ] Fond d'écran jaune uniforme `#FFE333`
- [ ] Image de fond `search_bg.png` avec 20% d'opacité
- [ ] Header standardisé (back_icon + home_icon 50x50px)
- [ ] Padding vertical 24px et horizontal 24px

### 🔤 Typographie

- [ ] Titres en DelaGothicOne 38px
- [ ] Boutons en DelaGothicOne (24px primary/secondary, 19.2px tertiary)
- [ ] Inputs/labels en Chivo 18px
- [ ] Texte en MAJUSCULES pour titres et boutons

### 🎯 Boutons

- [ ] Utilisation des widgets `PrimaryButton`, `SecondaryButton`, `TertiaryButton`
- [ ] Icônes PNG appropriées (45x45px pour principaux, 40x40px pour dialogues)
- [ ] Hauteurs correctes (75px principaux, 50px dialogues)

### 📱 Interactions

- [ ] Navigation home fonctionnelle sur tous les écrans
- [ ] Bouton retour fonctionnel
- [ ] Loading states gérés pour les boutons async

## 🚫 À ÉVITER ABSOLUMENT

- ❌ Utiliser `ElevatedButton` ou `OutlinedButton` directement
- ❌ Définir des styles de boutons inline
- ❌ Mélanger les tailles de police des boutons
- ❌ Utiliser d'autres couleurs de fond que `#FFE333`
- ❌ Créer des headers différents entre écrans
- ❌ Utiliser des polices autres que DelaGothicOne et Chivo
- ❌ Oublier les icônes PNG dans les boutons
- ❌ Créer des `TextFormField` sans `CustomTextField`

## 🔄 Évolutions Futures

Cette architecture permet :

- **Maintenance centralisée** : Tous les styles dans 2 fichiers
- **Cohérence garantie** : Impossible de dévier des standards
- **Évolutivité** : Modification globale en un seul endroit
- **Performance** : Widgets réutilisables et optimisés
- **Lisibilité** : Code simplifié et expressif

---

_Dernière mise à jour : Janvier 2025_
_Version : 3.0 - Système de boutons globalisé + Couleur unifiée_
